<%= form_for(@comment) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "یادداشت خود را درباره شهید اینجا بنویسید." %>
   </div>
  <%= f.submit "ارسال", class: "btn btn-primary" %>
<% end %>
