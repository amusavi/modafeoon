  <div class="col-md-8">
    <% if @shahid.comments.any? %>
      <h3>Comments (<%= @shahid.comments.count %>)</h3>
      <ol class="comments">
        <%= render @comments %>
      </ol>
      <%= will_paginate @comments %>
    <% end %>
  </div>
